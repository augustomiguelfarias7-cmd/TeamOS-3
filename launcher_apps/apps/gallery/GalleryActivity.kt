package com.teamos.gallery

import android.os.Bundle
import androidx.appcompat.app.AppCompatActivity
import com.teamos.launcher.R

/**
 * App Galeria nativo - TeamOS 3.0
 * Visual: One UI 8 + Android 16 Material 3 Expressive
 */
class GalleryActivity : AppCompatActivity() {

    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_gallery)

        // Grid de imagens + visualização em tela cheia
        // Integração com a loja de papéis de parede
    }
}
